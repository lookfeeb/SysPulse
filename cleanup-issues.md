# 磁盘清理第二轮问题清单

更新时间：2026-08-14

状态说明：结果卡操作区与双层表头已重构；空工具链和“稍后审核”已移除；固定盘符与目录挂载的固定分区均纳入“浅层快速侦察 → 候选深入扫描”。后端热点扫描、结构化导出、有界并发和重复 I/O 修复均已完成，全部静态门槛通过。

## P0：必须先修复

- [x] 编程缓存弹窗完全替换为“工具链列表 + 路径详情”布局。
  - 工具链至少覆盖 Rust、Node.js、Python、Go、C/C++、.NET、Java。
  - 每条路径显示大小、文件数、命中规则、风险等级和来源（热点、用户目录、全盘发现或工具全局缓存）。
  - 路径和工具链选择均支持全选、半选和逐项切换。
- [x] 路径勾选状态持久化。
  - 使用规范化路径键保存排除状态。
  - 程序重启、重新扫描、路径使用 `\\?\` 前缀或大小写变化后仍能恢复。
  - 已不存在的路径选择要自动淘汰，新增路径采用所属类别的安全默认值。
- [x] 前后端彻底移除回收站。
  - 删除扫描类别、图标、清理分支、Shell API、Windows feature 和回收站专属测试。
  - 扫描结果与导出中不得再出现 `recycle-bin`。
- [x] 扫描和停止扫描合并为一个状态按钮。
  - 空闲：开始扫描；扫描中：停止扫描；取消中：禁用并显示正在停止。
  - 当前路径后紧跟扫描动画，不再单独占行。
- [x] 清理必须继续保持扫描快照校验。
  - 只允许完整、未取消、未过期的扫描结果进入清理。
  - 清理前重新验证目录身份、文件创建/修改时间和运行中工具链进程。
  - “战争蚁”只是清理执行器命名，不得绕过现有安全策略或无限并发删除。

## P1：扫描深度、速度与精确性

- [x] 将现有扫描器重构为有界“蚁群式调度”。
  - 蚁后：统一任务状态、取消令牌、预算、进度、去重和优先级。
  - 侦察蚁：并发枚举热点根、自定义项目根、用户目录和固定磁盘候选目录。
  - 工兵蚁：验证缓存规则、计算大小和文件数、建立或刷新热点索引。
  - 战争蚁：仅在用户确认后清理扫描快照中的路径。
  - 并发数必须根据 CPU、磁盘数量和队列积压动态限制；机械盘默认低并发，禁止无限创建线程。
- [x] Rust 编译缓存实现可靠全盘发现。
  - 优先扫描持久化热点、用户配置目录和用户添加的项目目录。
  - 后台侦察固定磁盘，识别 `Cargo.toml + target`、Cargo `target-dir` 和构建产物标记。
  - 跳过重解析点、系统目录、依赖仓库和不可信的同名 `target` 目录。
- [x] 覆盖用户目录。
  - 用户主目录作为独立根扫描，Desktop、Documents、Downloads、OneDrive、source/repos、projects、code、src、dev 作为高优先级根。
  - 防止相互包含的根被重复扫描。
- [x] 建立持久化热点索引。
  - 建议路径：`%LOCALAPPDATA%\\SysPulse\\cleanup-hotspots-v1.json`。
  - 至少保存：规范化路径、工具链、命中规则、最近命中时间、最近扫描时间、大小、文件数、连续失效次数。
  - 写入采用临时文件加原子替换；损坏文件可恢复为空索引。
  - 下次扫描热点优先；多次失效或不再通过规则验证的路径自动淘汰。
- [x] 改进扫描预算。
  - 当前全局 120 秒与 200 万项预算会把权限警告也判为整体不完整，需要区分致命截断与普通不可访问项。
  - 单个路径超深、超时或不可访问应记录路径级状态，不应无条件让全部结果不可清理。
  - 取消检查必须贯穿目录发现、大小统计和清理阶段。
- [x] 减少重复 I/O。
  - 项目发现与大小统计共享文件身份去重。
  - 多个类别命中同一路径时只读取一次，分类阶段复用测量结果。
  - 自定义根、热点根、用户根和卷根先规范化并消除父子重复。
  - 相同“规范化目录 + 保留期”只建立一个工兵测量任务；并发请求等待同一结果，不再因线程时序让后命中类别变成 0。
  - 扫描父根遇到已单独分配的子根时跳过该子树，避免侦察蚁重复遍历；最终同路径/同目录身份只归属第一个已验证类别。

## P1：项目目录管理

- [x] 保持最多 16 个目录并持久化。
- [x] 支持添加、逐项移除、替换/编辑和清空。
- [x] 去重时保留用户可读路径，比较时使用规范化键。
- [x] 后端拒绝不存在、文件、卷根和危险系统目录。
- [x] 修改目录配置后使当前扫描快照失效，提示重新扫描。

## P1：结构化导出

- [x] 新增 Rust IPC 导出命令，由前端保存对话框选择 JSON 文件。
- [x] 导出基于当前扫描快照，不重新扫描，也不执行清理。
- [x] 顶层包含版本、扫描标识/时间/耗时、完整性、警告、项目根、热点和工作线程摘要。
- [x] 每条记录包含类别、路径、大小、文件数、命中规则、风险、保留期、选择状态、来源和扫描时间。
- [x] 路径身份字段可选导出，但不得包含无关隐私信息或文件内容。
  - JSON schema v2 导出 `volumeSerial` 与 `fileId`；不读取或导出文件内容。
- [x] JSON 使用 UTF-8、稳定字段名和版本号，采用临时文件原子替换。

## P2：前端体验与可访问性

- [x] 扫描结果卡头部融合项目目录、导出、全选安全项和清理容量操作，并使用双层字段说明表头。
- [x] 编程缓存详情只显示实际发现路径的工具链，不再渲染空占位项；移除“稍后审核”。
- [x] 项目目录管理使用独立视图，显示 `当前数量/16` 和扫描范围说明。
- [x] 编程缓存弹窗采用响应式双栏/上下布局；长路径省略并可点击打开。
- [x] 扫描进度展示蚁后、侦察、工兵阶段；清理阶段显示战争蚁授权执行。
- [x] 扫描、导出和清理期间禁用冲突操作。
- [x] 导出按钮在没有扫描结果时禁用并提供提示。

## 当前代码定位

- `src/routes/CleanupPage.tsx`
  - 已改为顶部操作卡、三项概览和单一结果表。
  - 编程缓存详情为工具链导航 + 路径审核表。
  - 项目目录支持添加、替换、移除和清空。
- `src/routes/cleanupSelection.ts`
  - 已实现类别/路径选择与规范化持久化基础，可继续复用。
- `src-tauri/src/ipc/commands/cleanup_cmd.rs`
  - 已覆盖 Rust、Node、Python、Go、C/C++、.NET 和 Java。
  - 侦察与计量均为按 CPU 自适应的有界工作线程。
  - 用户目录、固定磁盘、自定义根和持久化热点按优先级参与发现。
  - 已实现结构化导出、命中规则和来源字段。

## 验收门槛

- [x] Rust 单元测试覆盖回收站移除、热点读写/淘汰、根去重、固定分区浅侦察提升、侦察调度、取消和安全清理。
- [x] 前端测试覆盖路径状态重启恢复、项目目录增删改、扫描按钮状态机和路径选择恢复；Rust 测试覆盖导出记录生成与身份字段。
- [x] `cargo test` 全部通过（56 项）。
- [x] `cargo clippy --all-targets -- -D warnings` 通过。
- [x] `npm test` 全部通过（13 项）。
- [x] TypeScript 检查和生产构建通过。
- [x] `git diff --check` 通过。
- [x] 浏览器无截图验收：1280px 与 900px 宽度无横向溢出，操作分组、双层表头、空工具链隐藏和按钮移除均符合要求。
- [x] 不执行真实用户缓存清理作为自动测试。
